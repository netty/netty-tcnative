<?xml version="1.0" encoding="ISO-8859-15"?>
<!--
  ~ Copyright 2014 The Netty Project
  ~
  ~ The Netty Project licenses this file to you under the Apache License,
  ~ version 2.0 (the "License"); you may not use this file except in compliance
  ~ with the License. You may obtain a copy of the License at:
  ~
  ~   http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
  ~ WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
  ~ License for the specific language governing permissions and limitations
  ~ under the License.
  -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.netty</groupId>
    <artifactId>netty-parent</artifactId>
    <version>4.0.18.Final</version>
  </parent>
  <artifactId>netty-tcnative</artifactId>
  <version>1.0.0.Final+1.1.30-SNAPSHOT</version>

  <name>Netty/TomcatNative</name>
  <packaging>jar</packaging>

  <properties>
    <checkstyle.skip>true</checkstyle.skip>
    <nativeSourceDirectory>${basedir}/src/main/c</nativeSourceDirectory>
    <libDirectory>${basedir}/target/classes/</libDirectory>
  </properties>

  <dependencies>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>netty-common</artifactId>
      <scope>compile</scope>
      <version>${project.parent.version}</version>
    </dependency>
  </dependencies>

  <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
      </resource>
    </resources>

    <plugins>
      <!-- Must be built from a supported platform -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>check-platform</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <evaluateBeanshell>
                  <message>Linux64 or osx64 is required to build: ${os.name}/${os.arch}</message>
                  <condition>
                    platform = "" + System.getProperty("os.name") + ":" + System.getProperty("os.arch");
                    platform = platform.toLowerCase(java.util.Locale.US).trim();

                    platform.matches("^(linux|mac os x).*:(amd64|x86_64)$")
                  </condition>
                </evaluateBeanshell>
              </rules>
              <fail>true</fail>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- This artifact uses a different package name. -->
      <plugin>
        <groupId>org.apache.felix</groupId>
        <artifactId>maven-bundle-plugin</artifactId>
        <executions>
          <execution>
            <id>generate-manifest</id>
            <phase>process-classes</phase>
            <goals>
              <goal>manifest</goal>
            </goals>
            <configuration>
              <supportedProjectTypes>
                <supportedProjectType>jar</supportedProjectType>
              </supportedProjectTypes>
              <instructions>
                <Export-Package>org.apache.tomcat.*</Export-Package>
              </instructions>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <artifactId>maven-jar-plugin</artifactId>
        <configuration>
          <classifier>${os.classifier}</classifier>
        </configuration>
      </plugin>
    </plugins>

    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.fusesource.hawtjni</groupId>
          <artifactId>maven-hawtjni-plugin</artifactId>
          <version>1.10</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <profiles>
    <profile>
      <id>linux64</id>
      <activation>
        <os>
          <name>Linux</name>
          <arch>amd64</arch>
        </os>
      </activation>
      <properties>
        <os.classifier>linux</os.classifier>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.fusesource.hawtjni</groupId>
            <artifactId>maven-hawtjni-plugin</artifactId>
            <executions>
              <execution>
                <id>build-linux64</id>
                <configuration>
                  <name>${project.artifactId}</name>
                  <buildDirectory>${project.build.directory}/linux64</buildDirectory>
                  <nativeSourceDirectory>${nativeSourceDirectory}</nativeSourceDirectory>
                  <libDirectory>${libDirectory}</libDirectory>
                  <configureArgs>
                    <arg>--with-arch=x86_64</arg>
                    <arg>--enable-ocsp</arg>
                  </configureArgs>
                  <platform>linux64</platform>
                  <forceConfigure>true</forceConfigure>
                  <forceAutogen>true</forceAutogen>
                </configuration>
                <goals>
                  <goal>generate</goal>
                  <goal>build</goal>
                </goals>
                <phase>compile</phase>
              </execution>
              <execution>
                <id>build-linux32</id>
                <configuration>
                  <buildDirectory>${project.build.directory}/linux32</buildDirectory>
                  <nativeSourceDirectory>${nativeSourceDirectory}</nativeSourceDirectory>
                  <libDirectory>${libDirectory}</libDirectory>
                  <name>${project.artifactId}</name>
                  <configureArgs>
                    <arg>--with-arch=i386</arg>
                    <arg>--enable-ocsp</arg>
                  </configureArgs>
                  <platform>linux32</platform>
                  <forceConfigure>true</forceConfigure>
                  <forceAutogen>true</forceAutogen>
                </configuration>
                <goals>
                  <goal>generate</goal>
                  <goal>build</goal>
                </goals>
                <phase>compile</phase>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>osx64</id>
      <activation>
        <os>
          <family>mac</family>
          <arch>x86_64</arch>
        </os>
      </activation>
      <properties>
        <os.classifier>osx</os.classifier>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.fusesource.hawtjni</groupId>
            <artifactId>maven-hawtjni-plugin</artifactId>
            <executions>
              <execution>
                <id>build-osx</id>
                <configuration>
                  <name>${project.artifactId}</name>
                  <buildDirectory>${project.build.directory}/osx</buildDirectory>
                  <nativeSourceDirectory>${nativeSourceDirectory}</nativeSourceDirectory>
                  <libDirectory>${libDirectory}</libDirectory>
                  <configureArgs>
                    <arg>--with-arch=x86_64</arg>
                    <arg>--enable-ocsp</arg>
                  </configureArgs>
                  <platform>osx64</platform>
                  <forceConfigure>true</forceConfigure>
                  <forceAutogen>true</forceAutogen>
                </configuration>
                <goals>
                  <goal>generate</goal>
                  <goal>build</goal>
                </goals>
                <phase>compile</phase>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>

